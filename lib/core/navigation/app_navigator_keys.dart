import 'package:flutter/material.dart';

/// Root navigator for opening routes from deep link handlers outside the widget tree.
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
